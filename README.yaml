---
#
# This is the canonical configuration for the `README.md`
# Run `make readme` to rebuild the `README.md`
#

# Name of this project
name: terraform-aws-ecr

# Tags of this project
tags:
  - aws
  - terraform
  - terraform-modules
  - codecommit

# Categories of this project
categories:
  - terraform-modules/CICD

# Logo for this project
#logo: docs/slalom-logo.png

# License of this project
license: "APACHE2"

# Copyrights
copyrights:
  - name: "Slalom, LLC"
    url: "https://slalom.com"
    year: "2019"

# Canonical GitHub repo
github_repo: jameswoolfenden/terraform-aws-ecr

# Badges to display
badges:
  - name: "Build Status"
    image: "https://api.travis-ci.com/JamesWoolfenden/terraform-aws-ecr.svg?branch=master"
    url: "https://travis-ci.com/JamesWoolfenden/terraform-aws-ecr"
  - name: "Latest Release"
    image: "https://img.shields.io/github/release/JamesWoolfenden/terraform-aws-ecr.svg"
    url: "https://github.com/JamesWoolfenden/terraform-aws-ecr/releases/latest"

related:
  - name: "terraform-aws-codebuild"
    description: "Making a Build pipeline"
    url: "https://github.com/jameswoolfenden/terraform-aws-codebuild"

# Short description of this project
description: |-
  Terraform module to provision an AWS [`Elastic Container Registry`](https://aws.amazon.com/ecr/).

# How to use this project
usage: |-
  Include this repository as a module in your existing terraform code:

  ```hcl
  module "ecr" {
    source           = "github.com/JamesWoolfenden/terraform-aws-ecr"
    name             = "${var.name}"
    region           = "${var.region}"
    repositorypolicy = "${data.aws_iam_policy_document.allowlocals.json}"
    depends_on       = ["${aws_iam_group.developer.group_name}"]
  }
  ```

include:
  - "docs/targets.md"
  - "docs/terraform.md"
  - "docs/usage.md"

# Contributors to this project
contributors:
  - name: "James Woolfenden"
    github: "jameswoolfenden"
